MANDATORY WORKFLOW:

STEP 1 — READ CONTEXT:
1. Read AGENT_CONTEXT.md — it lists every existing function, route, page, and model.
2. Read PROJECT_GUIDE.md — it defines the HUI design system and coding conventions.

STEP 2 — AUDIT BEFORE BUILDING:
3. BEFORE writing any code, check if this feature (or parts of it) already exists.
4. If it exists: review the current implementation, identify bugs or improvements, fix them.
5. If it partially exists: extend it — do NOT rebuild from scratch.
6. If it doesn't exist: build it following the conventions in the context files.

STEP 3 — CODING RULES:
7. Do NOT rebuild, re-scaffold, or duplicate anything that already exists.
8. ADD new server actions to src/lib/actions.ts (do NOT create new action files).
9. EXTEND existing pages/components when possible rather than creating new ones.

STEP 4 — VERIFY:
10. Run `npm run dev` and visually verify your changes work in the browser.
11. Run `npm run build` to confirm there are no build errors.

STEP 5 — DEPLOY:
12. Stage all changes: git add -A
13. Commit with a descriptive message: git commit -m "feat: <short description of what was built/fixed>"
14. Push to trigger Vercel deployment: git push origin main
15. Confirm the push succeeded and report the commit hash.

TASK: Build daily logs for projects.

Build:
1. PRISMA MODEL — Add DailyLog to schema.prisma:
   - id, projectId, date (DateTime), createdById (User)
   - weather: String (e.g., "Sunny, 75°F")
   - crewOnSite: String (comma-separated names or free text)
   - workPerformed: String (rich text / long text)
   - materialsDelivered: String?
   - issues: String?
   - photos: DailyLogPhoto[] (id, dailyLogId, url, caption)
   - createdAt, updatedAt

2. PROJECT UI at src/app/projects/[id]/daily-logs/page.tsx:
   - Timeline view: vertical list sorted by date (newest first)
   - Each log entry shows date header, weather icon, crew count, summary, photo thumbnails
   - "Add Log" button → form with all fields + multi-photo upload via Supabase
   - Click a log to expand full details

3. LOG ENTRY FORM:
   - Date picker (defaults to today)
   - Weather input with icon selector
   - Crew text area
   - Work performed (multi-line)
   - Materials delivered (optional)
   - Issues/delays (optional)
   - Photo upload grid (drag-and-drop, max 10 photos)

4. EXPORT — "Export PDF" button that generates a formatted daily log report.

Add "Daily Logs" to ProjectInnerSidebar.tsx. Gate behind "dailyLogs" permission. Use hui-card for each log entry, hui-input for form fields.
