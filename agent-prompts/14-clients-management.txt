MANDATORY WORKFLOW:

STEP 1 — READ CONTEXT:
1. Read AGENT_CONTEXT.md — it lists every existing function, route, page, and model.
2. Read PROJECT_GUIDE.md — it defines the HUI design system and coding conventions.

STEP 2 — AUDIT BEFORE BUILDING:
3. BEFORE writing any code, check if this feature (or parts of it) already exists.
4. If it exists: review the current implementation, identify bugs or improvements, fix them.
5. If it partially exists: extend it — do NOT rebuild from scratch.
6. If it doesn't exist: build it following the conventions in the context files.

STEP 3 — CODING RULES:
7. Do NOT rebuild, re-scaffold, or duplicate anything that already exists.
8. ADD new server actions to src/lib/actions.ts (do NOT create new action files).
9. EXTEND existing pages/components when possible rather than creating new ones.

STEP 4 — VERIFY:
10. Run `npm run dev` and visually verify your changes work in the browser.
11. Run `npm run build` to confirm there are no build errors.

STEP 5 — DEPLOY:
12. Stage all changes: git add -A
13. Commit with a descriptive message: git commit -m "feat: <short description of what was built/fixed>"
14. Push to trigger Vercel deployment: git push origin main
15. Confirm the push succeeded and report the commit hash.

TASK: Build a dedicated Clients management page.

Current state:
- Client model in prisma/schema.prisma: name, initials, email, companyName, phone, address fields, internalNotes
- Client CRUD API at src/app/api/clients/
- Clients are currently only created inline when creating leads/projects

Build:
1. CLIENTS LIST at src/app/clients/page.tsx:
   - Follow list page pattern (PROJECT_GUIDE.md §4.1)
   - Summary cards: Total Clients, Active (have open projects), Revenue (sum of related estimate totals)
   - Table: Name, Company, Email, Phone, Projects count, Total Revenue
   - Search and filter by name/company
   - "Add Client" button → modal with all Client fields

2. CLIENT DETAIL at src/app/clients/[id]/page.tsx:
   - Two-pane layout (§4.2): contact info left, activity right
   - Contact card with all client fields, editable inline
   - Tabs: Projects (linked projects list), Estimates, Invoices, Contracts
   - Activity timeline: recent estimates sent, invoices paid, contracts signed

3. SIDEBAR — Add "Clients" nav item to src/components/Sidebar.tsx (icon: person outline, permission-gated by a new "clients" permission or no gate).

4. LINK EXISTING — When creating a new Lead or Project, the client selector should search existing clients and offer "Create New" if not found.

Use hui-card for containers, Avatar component for client initials, StatusBadge for project statuses.
