MANDATORY WORKFLOW:

STEP 1 — READ CONTEXT:
1. Read AGENT_CONTEXT.md — it lists every existing function, route, page, and model.
2. Read PROJECT_GUIDE.md — it defines the HUI design system and coding conventions.

STEP 2 — AUDIT BEFORE BUILDING:
3. BEFORE writing any code, check if this feature (or parts of it) already exists.
4. If it exists: review the current implementation, identify bugs or improvements, fix them.
5. If it partially exists: extend it — do NOT rebuild from scratch.
6. If it doesn't exist: build it following the conventions in the context files.

STEP 3 — CODING RULES:
7. Do NOT rebuild, re-scaffold, or duplicate anything that already exists.
8. ADD new server actions to src/lib/actions.ts (do NOT create new action files).
9. EXTEND existing pages/components when possible rather than creating new ones.

STEP 4 — VERIFY:
10. Run `npm run dev` and visually verify your changes work in the browser.
11. Run `npm run build` to confirm there are no build errors.

STEP 5 — DEPLOY:
12. Stage all changes: git add -A
13. Commit with a descriptive message: git commit -m "feat: <short description of what was built/fixed>"
14. Push to trigger Vercel deployment: git push origin main
15. Confirm the push succeeded and report the commit hash.

TASK: Build a change order system.

Build:
1. PRISMA MODEL — Add ChangeOrder to schema.prisma:
   - id, projectId, estimateId (original estimate being amended)
   - code (auto-generated, e.g., CO-001), title, description
   - status: "Draft" | "Pending Approval" | "Approved" | "Rejected"
   - items: ChangeOrderItem[] (name, description, quantity, unitCost, total — same structure as EstimateItem)
   - totalAmount (positive = addition, negative = credit)
   - approvedBy, approvedAt, signatureUrl
   - createdAt, updatedAt

2. PROJECT UI at src/app/projects/[id]/change-orders/page.tsx:
   - List of change orders with status badges
   - "New Change Order" button → editor page
   - Editor: select base estimate, add/remove line items, auto-calculate total impact
   - Show running project total: Original Estimate + Sum of Approved COs

3. APPROVAL FLOW:
   - "Send for Approval" → emails client with portal link
   - Client views in portal, can approve + e-sign or reject with comment
   - On approval: update original estimate totals or create supplemental invoice

4. PORTAL VIEW — Add change orders to the client portal project view:
   - List of COs with status
   - Approve/reject buttons with signature pad (reuse SignaturePad.tsx)

Add "changeOrders" to ProjectInnerSidebar.tsx navigation. Gate behind the existing "changeOrders" permission.
