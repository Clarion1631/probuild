MANDATORY WORKFLOW:

STEP 1 — READ CONTEXT:
1. Read AGENT_CONTEXT.md — it lists every existing function, route, page, and model.
2. Read PROJECT_GUIDE.md — it defines the HUI design system and coding conventions.

STEP 2 — AUDIT BEFORE BUILDING:
3. BEFORE writing any code, check if this feature (or parts of it) already exists.
4. If it exists: review the current implementation, identify bugs or improvements, fix them.
5. If it partially exists: extend it — do NOT rebuild from scratch.
6. If it doesn't exist: build it following the conventions in the context files.

STEP 3 — CODING RULES:
7. Do NOT rebuild, re-scaffold, or duplicate anything that already exists.
8. ADD new server actions to src/lib/actions.ts (do NOT create new action files).
9. EXTEND existing pages/components when possible rather than creating new ones.

STEP 4 — VERIFY:
10. Run `npm run dev` and visually verify your changes work in the browser.
11. Run `npm run build` to confirm there are no build errors.

STEP 5 — DEPLOY (MANDATORY):
12. Stage all changes: git add -A
13. Commit with a descriptive message: git commit -m "feat: <short description of what was built/fixed>"
14. Push to trigger Vercel deployment: git push origin main
15. Confirm the push succeeded and report the commit hash.
16. Wait 30 seconds then verify the Vercel deployment URL is live (check https://probuild.vercel.app or the preview URL).

STEP 6 — TESTING HANDOFF (MANDATORY — do NOT skip this):
After deploying, you MUST provide the user with a detailed testing guide. Include ALL of the following:

17. SUMMARY — One paragraph explaining what was built/changed and where it lives.
18. VERCEL URL — Provide the live deployment URL where the user can test (e.g., https://probuild.vercel.app).
19. PAGES TO TEST — List every specific URL path the user should visit to test the feature. Use exact URLs like:
    - https://probuild.vercel.app/projects/[pick-a-project-id]/change-orders
    - https://probuild.vercel.app/portal/projects/[id]
    (Replace IDs with actual record IDs if known, or tell the user how to find them.)
20. STEP-BY-STEP TEST PLAN — Write a numbered checklist of exactly what to click, fill in, and verify. For example:
    a) Navigate to [URL]
    b) Click the "[Button Name]" button
    c) Fill in [field] with [sample value]
    d) Verify [expected result]
    e) Check [edge case]
21. EDGE CASES — List 2-3 edge cases the user should also test (empty states, error cases, permission checks, etc.).
22. DATABASE/SCHEMA CHANGES — If any Prisma migrations were added, note them and confirm they ran on the deployed environment.
23. ENV VARS — If any new environment variables are required, list them and tell the user where to add them (Vercel dashboard → Settings → Environment Variables).
24. KNOWN LIMITATIONS — Note anything that is not yet complete or has known issues.

TASK: Build the full invoicing workflow.

Current state:
- Invoice model exists in prisma/schema.prisma with PaymentSchedule relation
- Invoice has: code, projectId, clientId, status (Draft/Issued/Paid/Overdue), totalAmount, balanceDue
- Estimate model has EstimatePaymentSchedule for milestone payments
- Basic invoice list at src/app/projects/[id]/invoices/ (needs verification)
- Server actions in src/lib/actions.ts: createInvoiceFromEstimate, getInvoice, getProjectInvoices, recordPayment

Build these features:
1. INVOICE FROM ESTIMATE — Add a "Create Invoice" button on approved estimates that:
   - Creates an Invoice record linked to the project and client
   - Copies EstimatePaymentSchedule entries into PaymentSchedule records
   - Sets totalAmount from the estimate total
   - Auto-generates an invoice code (e.g., INV-001)

2. INVOICE DETAIL PAGE at src/app/projects/[id]/invoices/[invoiceId]/page.tsx:
   - Show invoice header (code, client, project, dates, status badge)
   - Payment schedule table with milestone name, amount, status, due date
   - "Record Payment" action that marks a PaymentSchedule as Paid and updates balanceDue
   - Status auto-transitions: all paid → "Paid", past due date → "Overdue"

3. INVOICE LIST at src/app/projects/[id]/invoices/page.tsx:
   - Summary cards at top: Total Invoiced, Total Paid, Outstanding Balance
   - Table with sortable columns: Code, Status, Amount, Balance Due, Issue Date
   - Status filter tabs

4. CROSS-PROJECT INVOICES LIST — Create a global invoices page at src/app/invoices/page.tsx or wire into the Financials sidebar link showing all invoices across projects.

Use hui-card, hui-btn, StatusBadge component. Follow the list page pattern from PROJECT_GUIDE.md §4.1.
