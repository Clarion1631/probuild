MANDATORY WORKFLOW:

STEP 1 — READ CONTEXT:
1. Read AGENT_CONTEXT.md — it lists every existing function, route, page, and model.
2. Read PROJECT_GUIDE.md — it defines the HUI design system and coding conventions.

STEP 2 — AUDIT BEFORE BUILDING:
3. BEFORE writing any code, check if this feature (or parts of it) already exists.
4. If it exists: review the current implementation, identify bugs or improvements, fix them.
5. If it partially exists: extend it — do NOT rebuild from scratch.
6. If it doesn't exist: build it following the conventions in the context files.

STEP 3 — CODING RULES:
7. Do NOT rebuild, re-scaffold, or duplicate anything that already exists.
8. ADD new server actions to src/lib/actions.ts (do NOT create new action files).
9. EXTEND existing pages/components when possible rather than creating new ones.

STEP 4 — VERIFY:
10. Run `npm run dev` and visually verify your changes work in the browser.
11. Run `npm run build` to confirm there are no build errors.

STEP 5 — DEPLOY:
12. Stage all changes: git add -A
13. Commit with a descriptive message: git commit -m "feat: <short description of what was built/fixed>"
14. Push to trigger Vercel deployment: git push origin main
15. Confirm the push succeeded and report the commit hash.

TASK: Build a budget vs. actual variance dashboard.

Current state:
- Budget model exists (totalLaborBudget, totalMaterialBudget, linked to Project + Estimate)
- TimeEntry tracks laborCost and burdenCost per project
- Expense tracks actual spend per estimate/item with cost codes
- CostCode model categorizes costs
- Project costing page exists at src/app/projects/[id]/costing/

Build:
1. VARIANCE DASHBOARD at src/app/projects/[id]/costing/page.tsx (enhance existing):
   - Summary cards: Total Budget, Actual Spend, Remaining, Variance %
   - Color-coded: green (under budget), yellow (80-100%), red (over budget)

2. COST CODE BREAKDOWN TABLE:
   - Rows per cost code used on the project
   - Columns: Cost Code, Budgeted (from EstimateItems), Actual (from TimeEntries + Expenses), Variance, % Used
   - Progress bars showing budget consumption
   - Sortable by variance (worst first)

3. CHARTS — Add visual charts using simple SVG or a lightweight chart approach:
   - Stacked bar chart: budget vs. actual by cost code
   - Trend line: cumulative spend over time vs. budget line

4. ALERTS — Show warning banners when:
   - Any cost code exceeds 90% of budget
   - Total project spend exceeds 85% of total budget

Use hui-card for chart containers, StatusBadge for status indicators.
