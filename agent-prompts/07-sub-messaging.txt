MANDATORY WORKFLOW:

STEP 1 — READ CONTEXT:
1. Read AGENT_CONTEXT.md — it lists every existing function, route, page, and model.
2. Read PROJECT_GUIDE.md — it defines the HUI design system and coding conventions.

STEP 2 — AUDIT BEFORE BUILDING:
3. BEFORE writing any code, check if this feature (or parts of it) already exists.
4. If it exists: review the current implementation, identify bugs or improvements, fix them.
5. If it partially exists: extend it — do NOT rebuild from scratch.
6. If it doesn't exist: build it following the conventions in the context files.

STEP 3 — CODING RULES:
7. Do NOT rebuild, re-scaffold, or duplicate anything that already exists.
8. ADD new server actions to src/lib/actions.ts (do NOT create new action files).
9. EXTEND existing pages/components when possible rather than creating new ones.

STEP 4 — VERIFY:
10. Run `npm run dev` and visually verify your changes work in the browser.
11. Run `npm run build` to confirm there are no build errors.

STEP 5 — DEPLOY:
12. Stage all changes: git add -A
13. Commit with a descriptive message: git commit -m "feat: <short description of what was built/fixed>"
14. Push to trigger Vercel deployment: git push origin main
15. Confirm the push succeeded and report the commit hash.

TASK: Build messaging between project managers and subcontractors.

Prerequisite: Prompt #5 (Client Messaging) should be done first — reuse the same MessageThread/Message models.

Build:
1. EXTEND MODELS — Update MessageThread to support sub threads:
   - Add optional subcontractorId field (nullable, links to Subcontractor)
   - A thread is either client-facing (projectId only) or sub-facing (projectId + subcontractorId)
   - Update Message.senderType to also accept "SUBCONTRACTOR"

2. API — Extend /api/messages:
   - Support ?subcontractorId=X filter to get sub-specific threads
   - POST accepts senderType "SUBCONTRACTOR"

3. ADMIN VIEW — In the project workspace, add a "Sub Messages" section:
   - Dropdown to select which subcontractor to message
   - Same chat UI as client messaging
   - Accessible from the project inner sidebar under the Messages tab (a sub-tab)

4. SUB-PORTAL VIEW — Add a Messages page at src/app/sub-portal/projects/[id]/messages/page.tsx:
   - Shows the sub's message thread for that project
   - Chat input to send messages
   - Use the same bubble UI style

5. EMAIL NOTIFICATIONS:
   - Sub sends message → email goes to CompanySettings.notificationEmail
   - PM sends message → email goes to Subcontractor.email

Follow HUI conventions. Reuse the chat components from Prompt #5 if applicable.
