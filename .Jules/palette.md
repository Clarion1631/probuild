## 2026-05-30 - Add ARIA Labels to FileBrowser icon buttons\n**Learning:** Many interactive icon buttons within FileBrowser lacked accessible names (e.g. view modes, modal close actions, option menus), hindering screen-reader and general a11y navigation.\n**Action:** Add `aria-label` attributes describing the purpose of every icon-only `<button>` so it accurately represents its functionality to assistive technologies.
